{
  "name": "dnsc",
  "version": "1.0.0",
  "description": "My personal website",
  "scripts": {
    "start": "hugo serve",
    "build": "hugo",
    "deploy:staging": "rm -rf /home/dennis/services/staging.dnsc.io/html && hugo --minify --buildDrafts && mv ./public/* ../services/staging.dnsc.io/html/.",
    "deploy:prod": "rm -rf /home/dennis/services/dnsc.io/html && hugo --minify && mv ./public/* ../services/dnsc.io/html/."
  },
  "author": "Dennis Schoepf",
  "devDependencies": {
    "@tailwindcss/aspect-ratio": "^0.4.2",
    "@tailwindcss/forms": "^0.5.6",
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.3.4"
  }
}
